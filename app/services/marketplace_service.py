
from app.models.user import User
from app.core.db import engine
from app.core.config import DATABASE_URL
from app.models.user import User

def create_item(username: str, item_name: str, price: float):
    return True

def accept_offer(item_name: str, username: str):
    return True
